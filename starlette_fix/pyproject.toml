[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "starlette-fix"
version = "1.0.0"
description = "Patches GZipResponder.__init__ so Streamlit works with Starlette >=0.41"

[tool.setuptools]
py-modules = ["sitecustomize"]
